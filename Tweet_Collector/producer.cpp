#include "tweetoscopeCollectorParams.hpp"
#include "tweet.hpp"
int main(int argc, char *argv[])
{

    auto key = std::to_string(int_key);
    builder.key(key);
    std::ostringstream ostr;
    ostr << ... the JSON message auto msg = ostr.str();
    builder.payload(msg);
    producer.produce(builder);

    return 0;
}